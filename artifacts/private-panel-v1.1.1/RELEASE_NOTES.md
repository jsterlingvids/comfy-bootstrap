# Private ComfyUI Panel distribution v1.1.1

This immutable bootstrap successor distributes the certified Panel source at `c33cce24d672fc22f7efb10f8d58d352d7753911` as a complete Git bundle with SHA-256 `cca874a7bb9b08450cdcfedbc5a63480ab9da0c6a01e3bf322b9e8350cd06115`.

The durable Hermes–Comfy bridge is deployed separately from certified commit `89d8436793da86fcdb4eb80555f1580616701620`. No bridge source, bridge capability, provider credential, or runtime secret is included in the bootstrap.

Bootstrap continues to reject Panel repository, commit, and checksum overrides that differ from the immutable release pins.

## Scope

This release publishes the bootstrap distribution and deploys the durable loopback bridge. Vast template creation, GPU launch, and fresh browser acceptance are intentionally **not run** and must not be inferred from this publication.

## Rollback

The predecessor Panel bundle (`28d50b3`, SHA-256 `56e1e75f9e07c089991362ccd06b8ed9c3dfca59abccfbc7a042678921b16656`) is preserved outside the repository as a non-published rollback artifact.
